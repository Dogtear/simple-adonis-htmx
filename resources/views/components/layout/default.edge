<!DOCTYPE html>
<html>
    <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">


    @if($slots.meta) 
        {{{await $slots.meta()}}}
    @endif

    <title>{{title || "Adonis.JS Movie Web App - No Title "}}</title>

    @vite(['resources/css/app.css', 'resources/js/app.js'])
    </head>
    <body>
     <div div class="max-w-3xl mx-auto mt-6">
        {{--  @include('partials/nav')  --}}

        {{{await $slots.main()}}}
        </div>
    </body>
</html>